<Window x:Class="TextLocator.AreaEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextLocator"
        mc:Ignorable="d"
        Title="Area Edit" Height="465" Width="830" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="CanMinimize" Icon="/Resource/App.ico" Loaded="Window_Loaded" >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- 上方内容区域 -->
            <RowDefinition Height="Auto" />
            <!-- 按钮区域 -->
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <!-- 文字提示 -->
            <TextBlock Text="Please name the area and choose a folder to save." FontWeight="Bold" Foreground="OrangeRed"/>
            <!-- 区域名称 -->
            <Grid Margin="0,10,0,0" VerticalAlignment="Center">
                <TextBlock Text="Area Name: " Margin="0,0,710,0" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBox x:Name="AreaName" Margin="100,0,0,0" Width="440" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            </Grid>
            <!-- 区域名称 -->
            <Grid Margin="0,10,0,0" VerticalAlignment="Center">
                <TextBlock Text="Area Folder: " Margin="0,10,710,0" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                <ListBox x:Name="AreaFolders" Margin="100,0,0,0" Height="140">
                    <ListBoxItem>
                        <Grid Width="440" Height="30">
                            <TextBlock Text="C:\Users\{User}\Document" Margin="0,0,0,0" FontSize="14" HorizontalAlignment="Left"/>
                            <Button Content="Delete" Margin="0,0,10,0" Height="20" Background="Red" Foreground="White" HorizontalAlignment="Right"/>
                            <!-- 分割线 -->
                            <Line X1="0" Y1="0" X2="430" Y2="0" VerticalAlignment="Bottom" Stroke="Gray" StrokeThickness="1"></Line>
                        </Grid>
                    </ListBoxItem>
                </ListBox>
            </Grid>
            <!-- 搜索文件类型 -->
            <StackPanel Margin="0,10,0,0" VerticalAlignment="Center">
                <TextBlock Text="File Types：" Margin="0,10,710,0" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                <WrapPanel x:Name="AreaFileTypes" Orientation="Horizontal" >
                    <CheckBox Content="Word Documents (doc, docx)" Height="20" Margin="10"/>
                    <CheckBox Content="Excel Spreadsheet (xls, xlsx)" Height="20" Margin="10"/>
                    <CheckBox Content="PPT Documents (ppt, pptx)" Height="20" Margin="10"/>
                    <CheckBox Content="PDF Documents (pdf)" Height="20" Margin="10"/>
                    <CheckBox Content="DOM Documents (html, xml)" Height="20" Margin="10"/>
                    <CheckBox Content="Text Documents (txt)" Height="20" Margin="10" Checked="CheckBox_Checked"/>
                    <CheckBox Content="Image Formats (jpg, png, gif, jpeg, bmp)" Height="20" Margin="10"/>
                    <CheckBox Content="Archive Formats (.rar, .zip, .7z, .tar, .jar)" Height="20" Margin="10"/>
                    <CheckBox Content="Source Code Files (.cs, .java, .js, .css, .md, .py, .c, .h, .cpp, .lua, .sql, .jsp, .json, .php, .rs, .rb, .yml, .yaml, .bat, .ps1)" Height="20" Margin="10"/>
                </WrapPanel>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,10,0,0" VerticalAlignment="Bottom">
            <Button x:Name="AddButton" Content="Add" Margin="0,0,0,0" Width="60" Click="AddButton_Click" Background="#2196f3" Foreground="White"/>
            <Button x:Name="SaveButton" Content="Save" Margin="0,0,10,0" HorizontalAlignment="Right" Width="60" Background="Green" Foreground="White" Click="SaveButton_Click"/>
            <Button x:Name="ExitButton" Content="Exit" Margin="150,0,0,0" Width="60" Background="Red" Click="ExitButton_Click" Visibility="Collapsed"/>
        </Grid>

    </Grid>
</Window>
